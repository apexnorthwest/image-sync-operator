[project]
name = "image-sync-operator"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2",
    "requests>=2",
    "kubedantic==0.1.1",
    "jinja2>=3.1.6",
]

[tool.ruff]
src = ["src"]
indent-width = 4
line-length = 120

[tool.ruff.format]
quote-style = "single"
